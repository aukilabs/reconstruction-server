{
    "name": "Reconstruction Server Devcontainer",
    "remoteUser": "reconstruction-server",
    "build": {
        "dockerfile": "../docker/Dockerfile.dev"
    },
    "workspaceFolder": "/app",
    "workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind",
    "customizations": {
        "vscode": {
            "extensions":[
                "golang.Go",
                "ms-python.python",
                "ms-python.debugpy"
            ]
        }
    },
    "forwardPorts": [8080],
    "runArgs": [
        "--gpus", "all",
        "--shm-size", "512m"
    ],
    "mounts": [
        // Modify this to your own data folder
        "source=/path/to/data,target=/data,type=bind"
    ],
    "postCreateCommand": ".devcontainer/build_golang_server.sh"
}